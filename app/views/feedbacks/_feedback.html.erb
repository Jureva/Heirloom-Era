<li id="feedback-<%= feedback.id %>">
  <%= link_to gravatar_for(feedback.customer, size: 50), feedback.customer %>
  <span class="customer"><%= link_to feedback.customer.name, feedback.customer %></span>
  <span class="content"><%= feedback.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(feedback.created_at) %> ago.
  </span>
</li>